/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Ta palette personnalisée
        dark: "#000000",      // Noir pur
        light: "#FFFFFF",     // Blanc pur
        gold: {
          DEFAULT: "#D4AF37", // Or classique
          light: "#F9F1D7",   // Or très clair pour les fonds ou survols
          dark: "#AA8A1E",    // Or plus sombre pour les bordures
        },
      },
    },
  },
  plugins: [],
}